import React, {use} from 'react';
import PropTypes from 'prop-types';

ColorBox.propTypes = {
    
};

function ColorBox() {
    return (
        <div>
            
        </div>
    );
}

export default ColorBox;