import React from 'react';
import PropTypes from 'prop-types';

Color.propTypes = {
    
};

function Color(props) {
    return (
        <div>
            
        </div>
    );
}

export default Color;