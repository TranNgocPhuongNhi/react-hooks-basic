import React from 'react';

Clock.propTypes = {};

function formatDate(date) {
    if(!date) return ''

    const hours = `0${date.getHours()
}

function Clock() {
    return (
        <div>
            
        </div>
    );
}

export default Clock;