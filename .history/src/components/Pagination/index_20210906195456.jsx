import React from 'react';
import PropTypes from 'prop-types';

Pagination.propTypes = {
    pagination: PropTypes.b
};

function Pagination(props) {
    return (
        <div>
            
        </div>
    );
}

export default Pagination;